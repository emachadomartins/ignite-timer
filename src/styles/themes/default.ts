import { ButtonVariant } from '../../components/Button/styles'

export const defaultTheme: Record<ButtonVariant, string> = {
  primary: 'purple',
  secondary: 'orange',
  danger: 'green',
  success: 'red',
}
